{% extends 'base.html.twig' %}

{% block title %}
    {{ 'delete.tag'|trans({'%id%': tag.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'delete.tag'|trans({'%id%': tag.id|default('')}) }} {{ tag.name }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('tag_index') }}" title="{{ 'tags.link'|trans }}">{{ 'tags.link'|trans }}</a>
    </div>
    {{ form_end(form) }}
{% endblock %}